name: smart_expense_tracker
description: Smart Expense Tracker - Fintech UI
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  fl_chart: ^1.2.0
  intl: ^0.20.2
  shared_preferences: ^2.2.2
  uuid: ^4.3.3
  image_picker: ^1.1.2
  share_plus: ^13.0.0
  path_provider: ^2.1.5
  firebase_core: ^4.7.0
  
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  hive_generator: ^2.0.1
  build_runner: ^2.4.13

flutter:
  uses-material-design: true
  assets:
    - assets/

flutter_launcher_icons:
  android: "launcher_icon"
  image_path: "assets/icon.jpg"
  web:
    generate: true
    image_path: "assets/icon.jpg"
    background_color: "#0B0211"
    theme_color: "#0B0211"
